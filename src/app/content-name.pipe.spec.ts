import { ContentNamePipe } from './content-name.pipe';

describe('ContentNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ContentNamePipe();
    expect(pipe).toBeTruthy();
  });
});
